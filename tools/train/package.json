{
  "type": "module",
  "private": true,
  "description": "Marks tools/train as ES modules so 'node tools/train/evolve.js' can run without affecting the rest of the project (browsers ignore package.json; this only changes how Node resolves files under this folder). See js/engine/package.json for the same pattern."
}
